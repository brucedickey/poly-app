<nav class="navbar fixed-top navbar-expand-sm sub_menu" style="postition: fixed; top: 90px;">
  <div class="container-fluid">
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav mr-auto">
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownCategories" 
            role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
            Categories
          </a> 
          <div class="dropdown-menu" aria-labelledby="navbarDropdownCategories">
            <%= link_to "All Categories", blog_categories_path, class: "dropdown-item" %>
            <% Blog::Category.all.each do |category| %>
              <%= link_to "#{category.name}", blog_category_path(category), class: "dropdown-item" %>
            <% end %>
            <% if logged_in? and current_user.admin? %>
              <div class="dropdown-divider"></div>
              <%= link_to "Create New Category", new_blog_category_path, class: "dropdown-item" %>
            <% end %>
          </div>
        </li>
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownArticles" role="button" 
            data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
            Articles
          </a>
          <div class="dropdown-menu" aria-labelledby="navbarDropdownArticles">
            <%= link_to "View All Articles", blog_articles_path, class: "dropdown-item" %>
            <% if logged_in? %>
              <div class="dropdown-divider"></div>
              <%= link_to "Create New Article", new_blog_article_path, class: "dropdown-item" %>
            <% end %>
          </div>
        </li>
      </ul>
      <form class="form-inline search-controls">
        <input class="form-control mr-sm-2 search-control" type="search" placeholder="Search" aria-label="Search">
        <button class="btn btn-sm btn-success search-control" type="submit">Search</button>
      </form>
    </div>
  </div>
</nav>
